# 1. Stage all changes (new files, modified files, and deleted files)
git add .

# 2. Create a commit with a descriptive message
git commit -m "Fix: fix some bug and end of the day "

# 3. Push the changes to your main branch
git push origin main